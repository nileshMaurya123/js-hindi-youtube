// ### 3. **Add an element to the end**

// > Add `"grape"` to the end of the `fruits` array using the appropriate array method.

function addElement() {
  let fruits = ["apple", "banana"];
  fruits.push("grape"); // use push, and "grape" should be a string in quotes
  console.log(fruits);
}

addElement();
